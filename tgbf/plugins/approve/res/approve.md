Approve a smart contract to spend a specific amount of TAU  
`/{{handle}} <contract name> <amount>`

List all smart contracts that you approved with the bot and show the currently approved amount  
`/{{handle}} list`